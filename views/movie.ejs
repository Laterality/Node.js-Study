<html>
	<head>
		<meta charset="utf-8"/>
		<title>EJS Example</title>
	</head>
	<body>
		<h1><%= title %></h1>
		<ul>
			<% movies.forEach(function(item)
			{ %>
				<li> <%= item.title %> / <%= item.director %> </li>
			<% }); %>
		</ul>
	</body>
</html>